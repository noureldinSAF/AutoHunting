# API Key Detection Service Configuration

# Server configuration
server:
  host: "0.0.0.0"
  port: 8000

# Hugging Face configuration (optional)
# This is for downloading models from Hugging Face Hub
huggingface:
  # Set this to your Hugging Face token if you need to access gated models
  token: "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"
  
  # Model configuration
  model:
    name: "bigcode/starpii"
    cache_dir: "/app/models"
    use_auth_token: true  # Set to true if using gated models

# Logging configuration
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
